
### To Get Started:
1. Run `composer install` and `npm install`.
2. Configure the database in the `.env` file use `DB_CONNECTION=sqlite` for easy setup.
3. on `.env` update `MAIL_MAILER` to `log` for Notification.
4. Execute `php artisan migrate --seed` to apply database migrations and seed the database with initial data.
